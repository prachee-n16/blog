The assignment operation ( $\leftarrow$ ) provides a convenient way to express complex queries by breaking them down into smaller steps. 
- Intermediate results are assigned to temporary relation variables. These assignments help in modularizing the query into smaller, manageable parts.
